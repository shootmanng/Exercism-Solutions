#if !defined(TRINARY_H)
#define TRINARY_H
#include <string>
#include <cmath>
#include <iostream>

namespace trinary {
    int to_decimal (std::string num);
}  // namespace trinary

#endif // TRINARY_H